create a user application that has a navigation bar and a footer across all pages, it enables user to register, login and loggout. create the pages below:

1. home page : a page that shows random stuffs, it should have a get started button that leads to the login page
2. login: login wih username and password
3. register: register user and store it in local storage
4. dashboard: see all registered users. and also have the email of the logged in user
5. user profile: see the details of the logged-in user